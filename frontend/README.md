# frontend

React app for app.nanko.dev (account-gated). Currently a bare Vite + React + TypeScript starter — the canvas (mode free) and its rendering library choice are deferred to the canvas-implementation pass.
