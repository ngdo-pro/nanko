# tests-e2e

Playwright suite running against `app.preprod.nanko.dev` (project `app`) and `library.preprod.nanko.dev` (project `library`). No test scenarios yet — user journeys will be added as `frontend`/`library` stabilize.
